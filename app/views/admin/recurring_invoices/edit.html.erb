<h2 class="text-2xl font-bold mb-6">Edit Recurring Invoice: <%= @recurring_invoice.client.name %></h2>

<div class="card bg-base-100 shadow max-w-xl">
  <div class="card-body">
    <%= render "form", recurring_invoice: @recurring_invoice, available_clients: @available_clients %>
  </div>
</div>
